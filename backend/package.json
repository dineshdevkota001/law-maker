{
  "name": "@backend",
  "version": "1.0.0",
  "description": "Python backend API for law-maker services.",
  "type": "module",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python scripts/build_assets.py"
  },
  "dependencies": {},
  "devDependencies": {}
}